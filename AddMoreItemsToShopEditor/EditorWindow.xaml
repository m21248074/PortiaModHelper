<Window x:Class="AddMoreItemsToShopEditor.EditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AddMoreItemsToShopEditor"
        mc:Ignorable="d"
        Title="AddMoreItemsToShop Editor" Height="600" Width="850">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Editor Workspace" FontSize="20" FontWeight="Bold" Foreground="#333333"/>
            <TextBlock Text="Active JSON Path:" Margin="0,10,0,5" FontWeight="SemiBold" Foreground="#555555"/>
            <TextBox x:Name="TxtPassedJsonPath" IsReadOnly="True" Background="#EAEAEA" Padding="5" Margin="0,0,0,15"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Loaded Mod Items Validation Table:" FontSize="14" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,5"/>

        <DataGrid x:Name="DgModItems" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  RowBackground="White" 
                  AlternatingRowBackground="#F9F9F9" 
                  GridLinesVisibility="Horizontal" 
                  BorderBrush="#CCCCCC" 
                  Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item ID" Binding="{Binding ID}" Width="80"/>
                <DataGridTextColumn Header="English Name" Binding="{Binding EnglishName}" Width="150"/>
                <DataGridTextColumn Header="Local Name" Binding="{Binding TargetLangName}" Width="150"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Count}" Width="70"/>
                <DataGridTextColumn Header="Store ID" Binding="{Binding StoreId}" Width="70"/>
                <DataGridTextColumn Header="Chance" Binding="{Binding Chance}" Width="70"/>
                <DataGridTextColumn Header="Game Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Store Name" Binding="{Binding StoreName}" Width="*"/>
                <DataGridTextColumn Header="Stroe Open Time" Binding="{Binding OpenTime}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
